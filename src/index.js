import ReactDOM from 'react-dom';
import './index.css'
import App from './App'

//render means basically we are writing this code to return it to our webpage
ReactDOM.render(
     <App />,
     document.getElementById('root')
);